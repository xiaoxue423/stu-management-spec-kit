# 功能规范: 新增与编辑学生成绩录入

**功能分支**: `feature/SPEC-202603271633/002-add-student-score-entry`
**创建时间**: 2026-03-27
**状态**: 草稿
**输入**: 用户描述: "目前系统有个漏洞，没有输入数据的入口。所以我还需要一个创建学生基本信息和月考成绩的功能，点击“新建”按钮，打开一个弹窗，可以输入学生的姓名、学号、分数；可以选择学生的性别(男/女)、可以选择月份（1-12月）、可以选择科目（数学、语文、英语暂定这3个），在学生列表页面 有一列操作列，有个编辑可以，点击编辑按钮，一样可以打开弹窗，并且回显所有的学生基本信息和科目成绩。"

## Clarifications

### Session 2026-03-27

- Q: 分数字段是否允许小数？ → A: 允许小数。

### Session 2026-04-01

- Q: 新建学生弹窗中的性别下拉选项文案应如何展示？ → A: 必须使用中文选项“男”和“女”。
- Q: 新建学生弹窗中的学号应如何录入？ → A: 学号不由用户输入，改为后端按创建顺序自动生成（0001-9999）。
- Q: 新建学生时成绩是否必须填写？ → A: 新建时仅姓名和性别必填，成绩可为空，后续可在编辑时补录或更新。
- Q: 创建学生与查询学生列表接口是否需要拆分？ → A: 需要拆分为两个独立接口，分别承担写入与查询职责。

## 用户场景与测试 *(必填)*

<!--
  重要说明: 用户故事应按重要性排序, 作为用户旅程进行优先级划分.
  每个用户故事/旅程必须能够独立测试——这意味着即使只实现其中一个, 
  你仍然应该有一个可行的 MVP(最小可行产品)来交付价值.

  为每个故事分配优先级(P1、P2、P3 等), 其中 P1 是最关键的.
  将每个故事视为独立的功能切片, 可以: 
  - 独立开发
  - 独立测试
  - 独立部署
  - 独立向用户演示
-->

### 用户故事 1 - 新建学生与成绩记录 (优先级: P1)

作为 教务老师，我想要 通过“新建”弹窗录入学生基本信息和月考成绩，以便让系统具备可持续的数据输入能力。

**优先级原因**: 当前系统缺少录入入口，这是阻断业务闭环的关键问题；不解决则后续查询和统计无数据来源。

**独立测试**: 点击“新建”按钮，仅填写姓名并选择性别即可提交，系统自动生成学号并在列表显示新增学生；后续进入编辑可补录成绩，即可独立验证价值。

**验收场景**: 

1. **当** 用户点击学生列表页“新建”按钮 **时** 学生管理系统 **应当** 打开录入弹窗。
2. **当** 用户在弹窗中仅填写姓名并选择性别后提交 **时** 学生管理系统 **应当** 自动生成学号并保存显示新增数据。
3. **如果** 用户缺少姓名或性别等必填项 **则** 学生管理系统 **应当** 给出明确校验提示并阻止提交。
4. **当** 用户在新建时不填写成绩信息 **时** 学生管理系统 **应当** 允许提交，并支持后续在编辑时补录成绩。
5. **在** 用户成功提交后 **处** 学生管理系统 **应当** 关闭弹窗并在列表中展示最新数据。

---

### 用户故事 2 - 编辑并回显学生成绩信息 (优先级: P2)

作为 教务老师，我想要 通过操作列“编辑”修改学生信息和成绩，并自动回显原有数据，以便快速修正录入错误。

**优先级原因**: 录入后可能存在错误，若不能编辑将导致数据长期不准确，影响查询可信度。

**独立测试**: 在学生列表点击“编辑”，检查弹窗已回显学生姓名、学号、性别及科目成绩；修改后提交并刷新仍保持更新结果，且学号不被修改。

**验收场景**: 

1. **当** 用户点击某学生所在行的“编辑”按钮 **时** 学生管理系统 **应当** 打开编辑弹窗并回显完整已有信息。
2. **如果** 用户仅修改部分字段后提交 **则** 学生管理系统 **应当** 保留未修改字段并仅更新变更内容。
3. **当** 用户保存成功 **时** 学生管理系统 **应当** 在学生列表中展示最新信息且无需重复录入。
4. **当** 用户在编辑弹窗提交修改 **时** 学生管理系统 **应当** 保持原学号不变。

---

### 用户故事 3 - 受限选项确保录入一致性 (优先级: P3)

作为 教务老师，我想要 使用预设选项选择性别、月份和科目，以便减少随意输入导致的数据不一致。

**优先级原因**: 该能力提升数据质量并降低后续维护成本，但依赖录入与编辑主流程先可用。

**独立测试**: 打开新建/编辑弹窗，验证性别仅可选男或女、月份仅可选 1-12 月、科目仅可选数学/语文/英语。

**验收场景**: 

1. **当** 用户打开新建或编辑弹窗 **时** 学生管理系统 **应当** 提供性别、月份、科目的限定选项。
2. **如果** 用户尝试提交不在限定范围内的选项值 **则** 学生管理系统 **应当** 拒绝提交并提示可选范围。
3. **当** 用户选择任一合法选项并提交 **时** 学生管理系统 **应当** 正常保存该选项值。
4. **当** 用户展开性别下拉 **时** 学生管理系统 **应当** 仅展示中文文案“男”“女”，且不展示英文文案。

---

### 边界情况

- 当连续创建学生时，系统如何按顺序生成 4 位学号（0001、0002...）。
- 当学号序列达到 9999 后继续新建时，系统如何阻止创建并提示号段已满。
- 当新建时成绩留空，系统如何允许提交且不生成非法成绩记录。
- 当编辑时分数为负数、超过满分或非数字时，系统如何提示并阻止提交。
- 当编辑时分数小数位超过 2 位时，系统如何提示并阻止提交。
- 当用户在编辑弹窗中打开后未修改直接保存，系统如何处理“无变更”场景。
- 当已有历史记录中科目超出当前暂定范围时，系统如何在编辑时展示并引导修正。

## 需求 *(必填)*

<!--
  需要操作: 本节内容表示占位符.
  请用正确的功能需求填写.
-->

### 功能需求

- **FR-001**: 系统必须在学生列表页提供“新建”入口，并可打开录入弹窗。
- **FR-002**: 系统必须允许用户在新建弹窗中输入学生姓名（学号不提供手工输入，成绩允许为空）。
- **FR-003**: 系统必须允许用户在新建弹窗中选择学生性别（男/女）。
- **FR-004**: 系统必须允许用户在新建弹窗中选择月份（1-12 月）。
- **FR-005**: 系统必须允许用户在新建弹窗中选择科目（数学、语文、英语）。
- **FR-006**: 系统必须在学生列表页提供操作列与“编辑”入口，并支持打开编辑弹窗。
- **FR-007**: 系统必须在编辑弹窗中回显学生基本信息与已记录的科目成绩信息。
- **FR-008**: 系统必须支持用户提交编辑后的数据，并在列表中显示更新结果。
- **FR-009**: 系统必须对必填字段、学号唯一性进行校验并在失败时阻止提交。
- **FR-010**: 系统必须在编辑成绩时校验分数合法性，分数格式限制为 0-100 范围内最多 2 位小数。
- **FR-011**: 系统必须在新建学生弹窗的性别下拉中仅展示中文选项“男”“女”，不出现英文性别文案。
- **FR-012**: 系统必须在新建学生时由后端自动生成学号，格式为 4 位数字（0001-9999），并按创建顺序递增。
- **FR-013**: 系统必须在学号序列达到 9999 后拒绝新建并给出可理解提示。
- **FR-014**: 系统必须在编辑学生流程中保持学号只读，不允许用户修改。
- **FR-015**: 系统必须允许新建学生时成绩为空，并在编辑流程中补录或更新成绩。
- **FR-016**: 系统必须将“创建学生”与“查询学生列表”定义为两个独立接口，创建接口仅负责写入，列表接口仅负责查询。
- **FR-017**: 系统必须通过持久化存储保存新建与编辑所提交的学生档案与成绩数据，使常规服务重启后用户仍能看到与重启前一致的已提交内容（具体要求见 `specs/005-mysql-db-persistence/spec.md`）。

### 非功能需求 *(必填)*

- **NFR-001 代码质量**: 变更后的代码必须保持可读、可维护，并通过既定质量检查。
- **NFR-002 测试标准**: 关键路径与边界场景必须可测试，缺陷修复必须附带回归验证。
- **NFR-003 体验一致性**: 用户可见交互、文案和反馈必须与现有产品体验一致。
- **NFR-004 性能要求**: 95% 的弹窗打开操作应在 1 秒内完成可交互，95% 的新建或编辑提交应在 2 秒内返回结果。
- **NFR-005 范围控制**: 不实现未被明确需求驱动的复杂能力，遵循 YAGNI 与禁止过度设计原则。

### 关键实体 *(如果功能涉及数据则包含)*

- **学生档案**: 代表学生基础身份信息，关键属性包括姓名、学号、性别；学号由系统自动生成并用于唯一标识学生。
- **月考成绩记录**: 代表学生某月份某科目的成绩，关键属性包括所属学生、月份、科目、分数；与学生档案为一对多关系。

## 成功标准 *(必填)*

<!--
  需要操作: 定义可衡量的成功标准.
  这些标准必须与技术无关且可衡量.
-->

### 可衡量的结果

- **SC-001**: 90% 的教务用户可在 1 分钟内完成一条新建学生记录（仅姓名+性别）。
- **SC-002**: 95% 的编辑操作可在 2 分钟内完成“打开弹窗-修改-保存”全流程。
- **SC-003**: 在验收测试中，新建与编辑流程的表单校验正确率达到 100%（非法输入均被拦截并提示）。
- **SC-004**: 上线后一个月内，因“无录入入口导致无法维护数据”的反馈数量降为 0。
- **SC-005**: 在新建学生流程验收中，100% 场景下性别下拉均显示中文“男”“女”且无英文选项。
- **SC-006**: 在验收测试中，连续新建 20 名学生时，学号必须严格按顺序生成且格式为 4 位数字。
- **SC-007**: 当学号达到上限后，100% 触发“号段已满”阻断提示，且不会产生异常脏数据。
- **SC-008**: 在验收测试中，新建时成绩留空的场景成功率达到 100%，且可在后续编辑中补录成功。

## 用户直接提供的技术细节 *(如果存在的话则包含，且无信息损失)*

- **交互方式**：点击“新建”打开弹窗录入；点击“编辑”打开弹窗回显并修改。
- **录入字段**：新建时仅姓名、性别（男/女）必填，成绩信息可空；编辑时可录入/更新月份（1-12 月）、科目（数学、语文、英语）、分数。学号不手工录入。
- **文案要求**：新建学生弹窗中，性别下拉菜单选项必须为中文“男”和“女”。
- **学号规则**：学号由后端自动生成，按创建顺序递增，格式 0001-9999。
- **接口拆分要求**：创建学生接口与查询学生列表接口必须拆分并独立演进。
- **列表要求**：学生列表存在操作列，包含“编辑”按钮。
- **持久化要求**：后端须使用 MySQL 等真实数据库持久落库，替代仅进程内内存存储；验收口径见 `specs/005-mysql-db-persistence/spec.md`。

## 假设

- 当前阶段一个成绩记录对应“单月 + 单科 + 单分值”，不涉及同月同科多次考试。
- 分数采用常见百分制校验范围（0-100），并允许最多 2 位小数；若业务后续调整满分体系或精度规则，作为新增需求处理。
- “科目暂定 3 个”按固定枚举处理，后续科目扩展不在本次范围。
- 学号生成基于当前最大值递增，采用 4 位左侧补零字符串表示。
- 新建后若无成绩，学生仍应在列表中正常展示，成绩在编辑流程补录。

## 范围守卫 *(必填)*

- 本次不实现: 批量导入导出、删除学生、复杂权限审批、多科目批量录入、成绩趋势图报表。
- 若需新增能力: 任何涉及删除、批量操作、统计报表或科目动态配置的需求，必须先由用户明确确认并补充验收标准。

## 实施验证附注（2026-04-01）

- 性能回归已按 `temp/quickstart.md` 执行：弹窗打开与提交时延满足 NFR-004 目标（95% 在阈值内）。
- 范围守卫复核通过：未新增批量、删除、报表、缓存、异步队列等超范围能力。

## 实施验证附注（2026-04-03）

- 已与 `specs/005-mysql-db-persistence` 对齐：补充 `FR-017` 与技术细节中的持久化要求，确保新建/编辑写入可经数据库在重启后复核。